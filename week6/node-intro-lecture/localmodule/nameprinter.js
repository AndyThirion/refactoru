// Prompt node to load in the names.js file and store its results into the names variable
var names = require('./names.js');

console.log(names);
console.log(names.names);
console.log(names.locations)

